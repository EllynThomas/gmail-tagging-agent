# OAuth credentials and tokens -- never commit these
credentials.json
token.json

# Personal label configuration -- use labels.default.json for the generic version
labels.json

# SSH private key -- never commit this
*.pem

# Python virtualenv
.venv/

# Tests
tests/

# Logs
*.log
